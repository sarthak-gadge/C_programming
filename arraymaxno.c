#include<stdio.h>
int main()
{
    int i,max=0,a[5];
    printf("enter the values in array :");
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }

    for(i=0;i<5;i++)
    {
        if(a[i] > max)
        {
            max = a[i];

















































            
        }
    }

    printf("maximum elements in array:%d",max);
    return 0;
    

}